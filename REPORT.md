## **Programming Assignment 1**

**Nama:** Fransisca Lintang Ayuningtyas  
**NRP:** 5024251109

Dalam tugas ini, saya melakukan beberapa modifikasi:
1. **Modifikasi `src/main.cpp`**: 
   - Menambahkan `#include <string>` dan menggunakan `std::getline(std::cin, nama)` agar program dapat menerima input nama lengkap dengan benar.
2. **Modifikasi `Makefile`**:
   - Mengubah variabel `APPNAME` dari `myapp` menjadi `MyApp`.
3. **Pengujian di WSL (Ubuntu)**:
   - Menjalankan perintah `make` untuk kompilasi dan tidak ada error. 
   - Program berhasil dijalankan dengan perintah `./MyApp`.


Program dapat berjalan dengan baik dan menampilkan output:  
`Hello [Nama]`